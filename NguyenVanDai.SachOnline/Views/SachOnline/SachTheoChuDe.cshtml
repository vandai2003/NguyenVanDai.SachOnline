@model IEnumerable<NguyenVanDai.SachOnline.Models.SACH>
@{
    ViewBag.Title = "Sachtheochude";
    Layout = "~/Views/Shared/_LayoutUse.cshtml";
}
<h2 class="text-center">SÁCH THEO CHỦ ĐỀ</h2>
<BR>
<div class="row text-center">
    @foreach (var sach in Model)
    {
        <div class="col-sm-4 col-md-4 col-lg-4 col-xs-6">
            <div class="thumbail">
                <img src="~/Images/@sach.AnhBia" alt="@sach.TenSach"
                     class="img-responsive img-rouded imgbook">
                <div class="caption">
                    <h4 style="min-height:70px">
                        <a href="@Url.Action("ChiTietSach","SachOnline", new { id = sach.MaSach }) ">@sach.TenSach</a>
                    </h4>
                    <p>
                        <a href="@Url.Action("ThemGioHang","GioHang", new { ms = sach.MaSach, url = Request.Url.ToString()})" class="btn btn-primary" role="button">
                            <span class="glyphicon glyphicon-shopping-cart" aria-hidden="true">

                            </span>Add to cart
                        </a>
                    </p>
                </div>
            </div>
        </div>
    }
</div>
